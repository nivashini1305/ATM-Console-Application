package ATM;

import java.util.ArrayList;

public class Admin extends Accounts {

    public Admin(String userId,String pin){//creates a constructor
       super(userId, pin);//super method which extends the variable from the accounts class
    }
}
