package ATM.ListOfNotes;

import ATM.Notes.Notes;

public class TwoHundred extends Notes {
    public TwoHundred(String note,long count){//constructor for the class TwoHundred
        super(note,count);//it uses super method because it extends the variable in the parent class
    }
}
